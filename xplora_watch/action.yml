name: 'Astra-Germany: Xplora® Watch'
description: 'Astra-Germany: Xplora® Watch'
runs:
  using: "composite"
  steps:
    - name: "Xplora Step 1"
      shell: python
      run: |
        "${{ github.action_path }}/entrypoint.py"
      id: step1
    - name:  "Xplora Step 2"
      shell: bash
      run: echo "${{ steps.get_square.outputs.repo }}"